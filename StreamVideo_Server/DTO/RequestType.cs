namespace StreamVideo_Server.DTO
{
    /// <summary>
    /// Enum xác định loại request client gửi lên server
    /// </summary>
    public enum RequestType
    {
        LOGIN,
        STREAM,
        LOGOUT
    }
}
